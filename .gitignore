node_modules
.next
out
dist
.env
.env.local
*.log
prisma/dev.db
prisma/dev.db-journal
.DS_Store
# internal notes / local artifacts
CHANGES.md
PATCH_INSTRUCTIONS.txt
deploymentSteps.md
serverSetting.txt
patch/
public/uploads/
# housekeeping inspection evidence — private, never served statically
private-uploads/
DOCUMENTATION.md
# Internal documentation — kept on disk, not versioned.
# NOTE: several code comments reference docs/ paths (e.g. src/lib/rbac.ts,
# src/app/api/voice/webhook/route.ts). Those files exist locally but will not
# be present in a fresh clone.
docs/
productGradePrompt.md
# Local .env backups — these contain REAL secrets in plaintext.
# Pattern-matched so future backups are covered without editing this file.
.env.bak.*
.env.backup*
.env.*.bak
